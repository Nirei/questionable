import Config

# Compile-time configuration. Phase 1 will populate this with Phoenix,
# Ash, and Oban settings. Kept in place so the production Dockerfile's
# `COPY config/` instruction has a stable surface to cache against.
