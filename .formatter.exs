# Used by `mix format`.
# Phoenix/Ash projects extend this via `import_deps:` once those deps are
# installed (see PLAN.md, Phase 1).
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}"
  ],
  line_length: 98,
  locals_without_parens: []
]
